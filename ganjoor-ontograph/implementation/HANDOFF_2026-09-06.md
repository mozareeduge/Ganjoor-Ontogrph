# HANDOFF - Ganjoor Ontograph v0.2 continuation

Date: 2026-09-06
Repo: `C:\Users\Zarinpal\Documents\Personal Formal Documents\Artworks\digital art\Ganjoor-Ontograph\Ganjoor-Ontogrph`
Branch: `ontograph/v0.1-build`

## Current State

- At audit start, git worktree was clean and aligned with `origin/ontograph/v0.1-build`. This audit introduced the handoff/progress reconciliation. U06 then updated only the researcher skill surface, its references/templates, `.claude/settings.json`, the U06 task status, and this handoff boundary. U07 then updated the researcher README/runbook, migration guide, changelog, and documentation test.
- Latest completed row before U06: W09B (`3d0e01131 Ontograph W09B: render inquiry history`). U06 is complete in local commits `4ea03b12e Ontograph U06: document governed researcher flow` plus the follow-up U06 fix commit; use `git log -1` for the exact fix hash after commit.
- Verified locally from `ganjoor-ontograph/`: focused U06/W05/W06/W08 regression run reports `28 passed`; full `.venv/Scripts/python.exe -m pytest -q` reports `328 passed, 10 skipped, 1 xfailed` after the U06 correction.
- `git diff --check` passes after U06.
- U08 packaging completed locally: `tests/test_u08_packaging.py -q` passed after its metadata-first failure; `python -m build` produced `ontograph-0.1.1.tar.gz` and `ontograph-0.1.1-py3-none-any.whl`; the clean venv wheel install ran `ontograph --help` outside the source checkout. Full suite: `330 passed, 10 skipped, 1 xfailed`.
- U09 adds .github/workflows/ontograph-ci.yml: least-privilege PR/push matrix for Ubuntu/Windows and Python 3.10/3.12. It installs dev extras, builds distributions, runs clean-wheel smoke, standalone release reconstruction, the full fixture suite, and scoped U09 Ruff lint/format. Static test failed before workflow then passed; YAML mapping and focused package/reconstruction/U09 tests: 3 passed; full local suite: 331 passed, 10 skipped, 1 xfailed. Remote matrix awaits GitHub execution.
- OpenSpec CLI is not installed on PATH. The official one-shot `npx --yes @fission-ai/openspec@latest validate v02-reality-tracked-update --strict --no-interactive` fetch produced no output and was stopped after 90 seconds during the prior audit, so strict CLI validation was unavailable in this environment. The delta specs were checked directly then; U06 did not change them.

## Completed Row Boundary

Completed in git:

```text
T01-T13
-> U03(base schemas only)
-> W01 -> W02 -> W03 -> W04A -> W04B -> W05 -> W06
-> U01 -> U02
-> W07A -> W07B -> W08
-> U04 -> U05 -> W09A -> W09B
-> U06 -> U07 -> U08 -> U09
```

The first remaining row is U10.

The older `HANDOFF_2026-09-04.md` is stale after W06. The historical `IMPLEMENTATION_LEDGER.md` is also intentionally not the live tracker for this OpenSpec continuation; it remains useful background but still contains its original P9.9 todo and old Phase 0-9 notes. Use `openspec/changes/v02-reality-tracked-update/tasks.md`, `V0_2_HERMES_EXECUTION_SPEC.md` Section 19, and git history for the current row boundary.

## Next Implementation Row

U07 completed: README/runbook, migration guide, changelog, and a discriminating documentation test now make the quickstart verbatim:

```text
study new
-> inquire
-> field/refresh
-> review
-> walk
-> assessed-full operation
-> source return
-> Finding
-> release
```

Do not reopen U06 unless the researcher skill itself needs correction. U06 verification added `ganjoor-ontograph/tests/test_u06_researcher_skill.py`: focused U06 file `4 passed`; focused cross-row regression set `28 passed`; full suite `328 passed, 10 skipped, 1 xfailed`.

The U06 follow-up closes the QA rejection: the replay now uses the literal `inquire --review â†’ walk` path, sole active situations are inherited, new promotion events use the canonical EventRecord shape, and existing append-only `kind` promotion rows are normalized only in memory for backward-compatible reads.

## Next Implementation Row

U10: implement the pinned clean-git corpus cache key from commit SHA + manifest hash + cache schema; use a full signal or explicit refusal for dirty/non-git corpora. Do not begin U11.

## Work System

Use two separate contexts:

- Implementation context: one row only, test-first, focused tests, full suite, `git diff --check`, one commit, then stop or continue to the next row.
- QA context: read-only. Start from this handoff, `openspec/changes/v02-reality-tracked-update/tasks.md`, `V0_2_HERMES_EXECUTION_SPEC.md`, and recent commits. Do not edit files, commit, or push from QA.

Human gates are still human-only:

- Gate G after U11: Mohammad reviews the ready-made Rostam flow and release.
- Gate H after V213: Mohammad reviews the final v0.2 scholarly releases and authorizes the final tag.

## Guardrails

- Never modify `poets/`, `index/`, or root `manifest.json`.
- Preserve `.hermes/`, `ontograph-workspaces/`, virtualenvs, caches, and SQLite indexes as local state; do not commit them.
- Do not edit completed rows in `IMPLEMENTATION_LEDGER.md` to make history look cleaner.
- Do not collapse multiple rows into one commit.
- Do not let renderer code compute research results.
- Do not let candidate proposals, raw Anchor Hits, co-incidence, or high support become evidence/relation claims without the governed route.
