# Implementation ledger

The single source of truth for "what's next." One `/ontograph-build`
iteration = find the first row with `Status: todo` (rows are already in
dependency order — do not skip ahead even if a later row looks easier),
implement it, run its Verify command, flip it to `done` only if Verify
passes, commit+push, stop. See
`../../.claude/skills/ontograph-build/SKILL.md` for the exact algorithm and
`BUILD_PLAN.md` for the phase rationale.

Never edit a `done` row's Verify command to make it pass — if a later task
reveals an earlier row's Verify was wrong or insufficient, add a new row
noting the correction; don't rewrite history silently.

Status values: `todo` · `in-progress` · `done` · `blocked` (with a Notes
entry explaining the blocker — never leave a row `blocked` silently).

**v0.1 stop condition reached.** Every Phase 0–7 row is `done`; no `todo`
or `blocked` rows remain anywhere in this ledger. `pytest tests/
fixtures/mini-ganjoor/test_fixture_ground_truth.py -q` reports 127
passed, 10 honestly-skipped (each with a named reason and, where
applicable, the ledger row that scoped the gap), 1 `xfail(strict=True)`
(Test F, mediation, v0.2 scope). `ontograph validate --gates --json`
reports all five spec §69 gates green against the fixture corpus. Per
`BUILD_PLAN.md`'s Definition of Done, the correct status is now "v0.1
apparatus, gates passed on fixture corpus, real-corpus integration
pending" — Phase 8 (manual, real-corpus smoke test) and the v0.2
reflexive relation layer (§72, mediation) remain and are out of this
ledger's automated scope.

## Phase 0 — Bootstrap and ground truth

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P0.1 | Fixture corpus + ground-truth test | §67 | `pytest fixtures/mini-ganjoor/test_fixture_ground_truth.py -q` | done | Rebuilt after external review (EXTERNAL_REVIEW.md, Findings 1–3): 4 poets, 27 poems, anchor/token-aware/assessed levels now distinguished; 7/7 pass. |
| P0.2 | `pyproject.toml` + `src/ontograph/` package skeleton (empty modules: corpus, normalize, field, anchors, census, encounters, metrics, compare, ablation, mediation, workspace, release, validate, cli — each with a module docstring citing its spec section, no logic yet) | §59 | `python -c "import ontograph"` succeeds after `pip install -e .` | done | Verified from a fresh venv (`/tmp/ontograph-venv`): install + import both succeed, `ontograph.__version__ == "0.1.0"`. |
| P0.3 | Pin dependencies (stdlib + PyYAML only, unless a task below needs more — do not add a dependency the spec doesn't require) | §59 | `pip install -e .` succeeds from a clean venv | done | Same verification as P0.2 — PyYAML>=6,<7 is the only pinned dependency; nothing else added. |
| P0.4 | `CorpusSnapshot` loader: reads a `manifest.json` root, records commit/path + manifest hash, exposes poet/poem counts | §56, §65 | unit test loads `fixtures/mini-ganjoor/manifest.json` and asserts `PoetsCount==4`, `PoemsCount==27` | done | `tests/test_corpus.py`, 4/4 pass. Commit is caller-supplied (module has no git dependency) — a caller wanting the §56 pin claim verified must pass a real `git rev-parse HEAD`, not assume one. |
| P0.5 | Real-schema spot check: confirm `VOrder`/`Position`/`CoupletIndex`/`SectionIndex1` semantics against a real ghazal AND a real epic-format poem | §21, §58 | inspect `poets/hafez/ghazal/sh1.json` (this repo's own root, now that it's a fork of the vendored corpus — no fetch needed) and `poets/ferdousi/shahname/12rokh/sh13.json` | **done** | **Real discrepancy found, not just confirmed clean.** The ghazal matches every fixture assumption exactly (1 Section, `SectionType: WholePoem`, `CoupletIndex`/`Position` as modeled). The epic poem does NOT: it has 116 `Sections` — one `SectionType: WholePoem` (`Index: 0`, `CoupletsCount: 115`, spanning the whole poem) PLUS 115 individual `SectionType: Couplet` entries (`Index: 2..116`, `CoupletsCount: 1` each, `Index: 1` inexplicably absent) — yet **every one of the 230 Verses still carries `SectionIndex1: 0`, uniformly**, regardless of which per-couplet Section it structurally belongs to. So `SectionIndex1` cannot distinguish per-couplet sections in epic-format poems at all; only `CoupletIndex` still works reliably. **Consequence for P1.3/P1.6/P3.6**: section-scale operations (§24) must not assume `SectionIndex1` identifies "the section a verse is in" for every `PoemFormat` — for epic/multi-section poems it identifies only the outer `WholePoem` section, and per-couplet Section records must be correlated via `CoupletIndex` (or ignored) instead. This is exactly the "structurally exceptional records are not silently forced into couplet logic" case §24 already warns about (see the fork's own `persian-poetry-mcp` skill notes on "epic poems... live in large section files" — the same warning, now confirmed at the field level). The mini-ganjoor fixture (all poems single-`WholePoem`-section) does not exercise this and should not be extended to fake it — real-corpus-only edge case, revisit in Phase 8 rather than adding a synthetic epic-format fixture poem on spec. |

## Phase 1 — Deterministic core

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P1.1 | Persian normalization pipeline: Yeh/Kaf variants, Unicode NFC, ZWNJ policy, optional diacritic strip, whitespace — versioned, original text+offsets preserved | §58 | round-trip test: normalize then map offsets back to original text on 5 hand-picked fixture verses, exact match | done | `tests/test_normalize.py`. Diacritic stripping is opt-in (off by default), matching the spec's treatment of anything that can alter which passages match. NFC is applied as a pre-pass, documented as operating on post-NFC "original" rather than true byte-for-byte input (see module docstring) — a named simplification, not a hidden one. |
| P1.2 | Token boundary definition for token-window scale (ZWNJ/half-space, punctuation, clitics) | §58 | unit test on poem 9107 (`آینه‌بند`, ZWNJ-joined compound) asserting the tokenizer treats it as ONE token distinct from `آینه` | done | `tokenize()` in `normalize.py`; ZWNJ is word-internal, punctuation is a boundary. `test_zwnj_joined_compound_is_one_token` passes directly against the poem-9107 case. Clitic segmentation beyond ZWNJ-joining is not attempted (out of v0.1 scope — would be a named transformation like stemming, spec §58). |
| P1.3 | `FieldCharter` + `ScopeSpec`: native filters (poet/category/poem/format/metre/rhyme/source), union/intersection/difference | §7, §23, §48 | build a field over `sample1 ∪ sample2` and assert poem count == 12; build `all minus sample3` and assert == 22 | done | `tests/test_field.py`, 5/5 pass. Only the `poet` atomic filter is implemented (category/format/metre/rhyme/source filters are straightforward extensions of the same `ScopeSpec.resolve` pattern, deferred to whichever later row first needs one — not needed yet by any Phase 2/3 test). |
| P1.4 | Derived fraction support: poet-life chronological proxy construction (rule stored with the field, never presented as a poem date) | §11, §23.2 | field built with a proxy rule shows `derived: true` and the exact rule string in its charter | done | Added `BirthYearLunarHijri`/`DeathYearLunarHijri` to the fixture's 4 poets (705-780, 750-820, 850-910, 600-655) specifically so a 700-800 AH proxy query has a real, verifiable 2-in/2-out split. A poet missing either boundary is excluded from the proxy, not assumed to overlap. |
| P1.5 | `LexicalAnchor` + exact `AnchorHit` census against a field | §8, §27.1 | census for anchor `آینه`+`آیینه` over the full fixture field returns exactly **7** hits (`token_aware_level` in ground truth), across poems `[9101,9102,9103,9104,9105,9106,9201]` — poem **9107 must NOT appear** | done | `tests/test_anchors.py`, matches token against exact tokens (not substrings) via `normalize.tokenize`. **Caught a real fixture bug while implementing this, not just an engine bug**: poem 9106's mirror occurrence had been accidentally written as `آینه` + `ی` concatenated with no separator (`آینهی`, an unintended ezafe attachment) instead of the standalone word — under correct token-exact matching this produced 6 hits, not 7, because `آینهی` != `آینه` as a token. Fixed by rewording 9106's text (`آینه در خاطره شکست و رفت`) so the word is a genuine standalone token again, matching what the ground truth always intended. Re-verified: all 7 fixture ground-truth tests plus all 5 anchor tests pass together. |
| P1.6 | SQLite derived research index (poets/categories/poems/sections/verses/couplets/normalized_verses/token_offsets) + manifest linking rows to source addresses | §57 | rebuild index from fixture, assert row counts match manifest counts exactly | done | `ontograph.corpus.build_index`, `tests/test_index.py` 4/4 pass. `categories` table deferred (no test needs it yet; the fixture's `_cat.json` files aren't consumed by any row before Phase 3) — add it when a category-scoped operation first needs it rather than now on spec. Row keys are the same poem_id/vorder the source JSON uses, which doubles as the source-address link the spec asks for — no separate provenance table needed. |
| P1.7 | Workspace-as-git-repo: `ontograph study new` initializes `ontograph-workspaces/<id>/` as its own git repo | §60 | after `study new`, `git -C <workspace> rev-parse HEAD` succeeds | done | `ontograph.workspace.new_study`, `tests/test_workspace.py` 4/4 pass. Refuses to clobber an existing workspace directory rather than silently overwriting one. |

## Phase 2 — Calibration and occurrence assessment

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P2.1 | Close Calibration sampler: stratified/random sample over Anchor Hits, seeded, context-ladder opening | §9 | seeded sample of size 5 over the 7 fixture mirror hits (token-aware level) is reproducible across two runs with the same seed | done | `ontograph.census.calibration_sample` + `open_context_ladder`; `tests/test_census.py` 7/7 pass (this row's tests among them). Stratified branch (`strata_key`) is implemented but not yet exercised by a test with real strata — no ledger row before Phase 3 needed one; flagged here rather than left silently untested. |
| P2.2 | `OccurrenceAssessment` + `OccurrencePolicy` records for BOTH mirror and rust; modes `anchor`/`assessed-full`/`assessed-rule`/`estimated` | §8.1, §49 | load `canonical-study-assessments.json` and apply it: mirror `assessed-full` census reports 5 accepted / 1 ambiguous / 1 rejected; rust `assessed-full` census reports 4 accepted / 0 / 0 | done | `ontograph.census.apply_assessments`/`accepted_poem_ids`/`ambiguous_only_poem_ids`. An unassessed hit is never silently treated as accepted (`apply_assessments` returns `None` for it, not a guessed decision). `OccurrencePolicy` as a named record type (vs. the plain dict/functions used here) deferred to whichever CLI/workspace row first needs to persist one — the mode logic itself is real and tested. |
| P2.3 | Ambiguous-hit denominator rule | §8.1.1 (v2.3.0) | prevalence under `assessed-full` on the fixture reports denominator 27 (not 26), numerator 5 (not 7), plus "1 unit with only ambiguous hits" (poem 9105) shown separately — matches `assessed_level.mirror_prevalence_poem_scale` ("5/27") in ground truth | done | `ontograph.census.assessed_full_prevalence` / `Prevalence`. Test asserts denominator 27, numerator 5, `ambiguous_only_count == 1`, matching ground truth exactly. |
| P2.4 | Default estimator: stratified proportion + Wilson score interval (+ finite-population correction above 10% sampling fraction) | §27.2, Appendix C.3 (v2.3.0) | on the **HEART object** (41 field-wide hits — see `heart_object` in ground truth, not the 7-hit mirror object), draw a seeded sample at a genuine ~50% sampling fraction (e.g. 20 of 41 hits), compute the Wilson interval, and assert the interval brackets the true prevalence (11/27 poems); repeat across ≥5 seeds and assert the interval brackets truth in ≥90% of runs | done | `ontograph.census.wilson_score_interval` + `estimate_incidence`. Tested at poem-level prevalence (15-of-27 poems sampled, >10% so the FPC branch is actually exercised — `est.fpc_applied is True` is asserted, not just assumed). Measured out-of-band at 200 seeds first (194/200 = 0.97 bracket rate) before setting the in-suite test to 30 seeds at the original ≥0.9 target, which it meets. 100%-sampling reduces exactly to the census, kept as a separate sanity test. |

## Phase 3 — Metrics and mapping operations

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P3.1 | Unit incidence, prevalence, spread, concentration | §27.3–27.6 | on fixture, prevalence(mirror, poem-scale, assessed-full) == 5/27 (per P2.3's corrected denominator); top-poet share reported alongside | **done** | `ontograph/metrics.py`: `unit_incidence()`, `spread()` (distinct poems=5, distinct poets=2 of 4), `concentration()` (top_poet="sample1", top_share=4/5, `counts_by_poet` always carried alongside per §27.6). `tests/test_metrics.py`: `test_prevalence_is_5_of_27_matching_p23` reuses P2.3's `assessed_full_prevalence` directly rather than re-deriving the denominator; `test_unit_incidence_binary`, `test_spread_across_two_poets`, `test_concentration_top_poet_is_sample1_at_80_percent`. |
| P3.2 | Dispersion (named, versioned Gries DP-family measure) shown only with raw counts + partition sizes | §27.7 | dispersion value never rendered without both raw counts in the same result object | **done** | `gries_dp()` implements Gries (2008) deviation-of-proportions over poet partitions (`DISPERSION_MEASURE = "gries-2008-dp"`); the `Dispersion` dataclass carries `raw_counts_by_poet` and `partition_sizes_by_poet` on every instance — there is no code path that returns a bare float. On fixture: DP≈0.514 for mirror's 5-poem-scale accepted occurrences (concentrated 4/5 in sample1, which is 7/27 of the corpus — asserted only as `0.5 < value < 0.6`, since the exact figure is a derived constant of the fixture rather than a hand-picked target). `test_dispersion_is_concentrated_and_carries_raw_counts`. |
| P3.3 | Typed co-incidence matrix (`AᵀA` from occurrence policy, not raw anchors) + separate `A_anchor` | §28.1 | on fixture: `A_anchor` (anchor-level) poem-scale mirror×rust co-incidence == **4**, couplet-scale == **3** (both include poem 9106). The assessed matrix (from `OccurrencePolicy`, per P2.2) poem-scale == **3**, couplet-scale == **2** (9106 excluded because its mirror hit is rejected). **Both numbers must be asserted, and they must differ** — a test that only checks one matrix cannot catch an engine that silently builds co-incidence from raw anchors instead of assessed occurrences. | **done** | `ontograph/compare.py`'s `typed_coincidence(mode=MODE_ANCHOR\|MODE_ASSESSED)`; `MODE_ASSESSED` raises `ValueError` if `accepted_a`/`accepted_b` are omitted rather than silently falling back to anchors. `tests/test_compare.py`: `test_anchor_mode_coincidence_is_4_poems_3_couplets`, `test_assessed_mode_coincidence_is_3_poems_2_couplets`, `test_anchor_and_assessed_coincidence_diverge_on_poem_9106` (asserts the two poem-scale sets are `!=` and that 9106 is in one, not the other — the direct Finding-1 regression guard), `test_assessed_mode_without_accepted_sets_raises`. Full suite: 51 passed. |
| P3.4 | Conditional association P(B\|A), P(A\|B) — labelled "conditional association," never causation | §28.2 | unit test asserts the two directions differ on the fixture and neither output string contains "cause" | **done** | `conditional_association()` on the assessed poem sets (accepted mirror=5, accepted rust=4, coincidence=3): `p_b_given_a=3/5`, `p_a_given_b=3/4`, asserted unequal. `ConditionalAssociation`'s dataclass field names are asserted to never contain "cause" (the docstring itself legitimately says "not causation" as a caller warning, so the guard checks field/output labelling, not the prose). |
| P3.5 | Lift with minimum-support guard; never called "statistical significance" without a declared reference condition | §28.3 | lift computation refuses (raises/labels) below the configured minimum support | **done** | `lift()` raises `InsufficientSupportError` (a `ValueError` subclass) when `support < minimum_support`, computing nothing rather than returning an unstable number. Tested both below-guard (mirror/rust support=3 < default minimum_support=5, raises) and at-guard (minimum_support=3, computes and checks the value against a hand-derived `p_a_and_b / (p_a * p_b)`). |
| P3.6 | Relation Scale Profile / `ScaleSurvival` across poem→section→couplet→verse→token-window | §29 | on fixture, **assessed-level** mirror-rust scale profile shows poem-scale 3, couplet-scale 2, correctly isolates 9102 as the poem that only survives broadly (poem-scale yes, couplet-scale no), AND correctly excludes 9106 from the couplet-scale count (present at poem/anchor scale, absent once occurrence assessment is applied) | **done, partial scope — flagged not picked** | `relation_scale_profile()` implements only the poem-scale and couplet-scale rungs of the full poem→section→couplet→verse→token-window ladder in §29; this is stated explicitly in the function's own docstring ("Verse-scale and token-window scale are not implemented in v0.1 (no ledger row before Phase 7 needs them)") rather than silently presented as the complete ladder. `test_scale_profile_assessed_mode_isolates_9102_as_poem_only` matches the target exactly: poem_scale=3, couplet_scale=2, poem_only_poem_ids={9102}. Section-scale is additionally blocked on P0.5's finding that `SectionIndex1` doesn't reliably identify per-couplet sections for epic-format poems — not exercised by this fixture (all single-`WholePoem`), revisit at Phase 8 alongside that finding. |
| P3.7 | Compare Fields (raw incidence, prevalence, dispersion/concentration deltas, companions gained/lost, close-reading cases behind the biggest difference) | §30 | compare `sample1`-field vs `sample2`-field; assert the report includes raw support alongside any ratio | **done, partial scope — flagged not picked** | `compare_fields()` implements only the raw-incidence + prevalence half of §30 (the part every downstream row needs first): `FieldComparison` always carries `incidence_a_field1`/`incidence_a_field2` alongside `prevalence_a_field1`/`prevalence_a_field2`, per §30's "a smoothed ratio is never the only number shown." Dispersion/concentration deltas, companions-gained/lost, and the close-reading-case selector are NOT implemented — no ledger row before Phase 7 needs them, and this is stated in the function's docstring rather than silently presented as the full §30 report. `test_compare_fields_carries_raw_incidence_alongside_prevalence` checks both numbers are present and correct for sample1 (7 poems) vs sample2 (5 poems) mirror-accepted subsets. |
| P3.8 | Ablation + `AblationRetention` | §31 | removing `sample1` (poems 9101–9107) from the full fixture field: **assessed** couplet-scale mirror-rust co-incidence drops from 2 to 1 (50% retention); **anchor-level** couplet-scale drops from 3 to 1 (33% retention). Both ratios must be computed and must differ — must match `manifest.json`'s `ablation_remove_sample1` block exactly | **done** | `ontograph/ablation.py`'s `ablation_retention()` reuses `compare.typed_coincidence()` on the field with the removed poems filtered out of both hit lists and accepted-sets, at the caller-declared mode — never re-derives co-incidence logic separately. Matches `manifest.json`'s `ablation_remove_sample1` block exactly: anchor-level 1/4 poem-scale, 1/3 couplet-scale; assessed-level 1/3 poem-scale, 1/2 couplet-scale. `tests/test_ablation.py`: one test per level plus `test_anchor_and_assessed_retention_diverge`, the direct Finding-1 regression guard for this row. `AblationRetention` always carries raw before/after counts alongside the ratio (never a bare percentage), per §31 and the same discipline as `Dispersion` (P3.2). Full suite: 54 passed. |
| P3.9 | Invariant audit — re-run the full Appendix A non-equivalence list as live assertions against the assembled engine (anchor≠object, co-incidence≠relation, estimate≠census, high frequency≠wide dispersion, etc.), using the fixture as the corpus under test | Appendix A | a dedicated `tests/invariants/test_appendix_a.py` passes, covering at minimum: anchor hit count (P1.5, 7) ≠ mirror-rust co-incidence numerator; `estimated`-mode HEART prevalence is never labelled "exact"; ablation retention is never phrased as an explanation ("caused by") | **done, scoped to what exists so far** | `tests/invariants/test_appendix_a.py`, 6 tests, all passing: the 3 minimum checks named in this row's target, plus `conditional association ≠ causation` (structural field/docstring guard) and `high frequency ≠ wide dispersion` (mirror's 5 accepted hits are 4/5-concentrated in one poet, DP>0.5, asserted directly against `metrics.py`'s raw counts rather than a bare aggregate). Appendix A names ~28 non-equivalences; only those with a real counterpart in the engine built through Phase 3 (corpus/normalize/field/anchors/census/metrics/compare/ablation/workspace) are checked — the module's own docstring states explicitly that Profile/Finding/Relation-Object/Mapping Object/Trace/Experiment/graph-projection/Reduction-Record items (Phase 4+) are out of scope here, not silently covered. This file must be **extended**, not just re-run, as those modules land, and re-checked again at P7.4 per this row's original instruction. |

## Phase 4 — Records, events, release

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P4.1 | Trace, Relation-Object, Profile, Experiment, Finding record CRUD (JSONL-backed, per workspace schema) | §49–55 | round-trip write/read for one instance of each record type | **done** | New module `ontograph/records.py`: `TraceRecord`, `RelationObject`, `ProfileRecord`, `ExperimentRecord`, `FindingRecord` dataclasses (field names match spec §50/§52/§54/§55 schemas), plus generic `write_record(workspace, record_type, record)`/`read_records(workspace, record_type)` writing/reading one JSON line per record under `research/<type>.jsonl` (the exact filenames spec §60's workspace layout names). `tests/test_records.py`: one round-trip test per record type, asserting dataclass equality survives the JSON write/read cycle. **Scope kept per Finding 5**: no Fourfold Diagnostic or Bridge Record classes exist in this module — the module docstring says so explicitly. |
| P4.2 | AI-summary Profile provenance fields (`summarizer_model_version`, `summarizer_prompt_version`) required when `access_apparatus=ai-summary` | §40, §52 (v2.3.0) | creating an ai-summary Profile without these fields raises/rejects | **done** | Enforced in `ProfileRecord.__post_init__` (raises `MissingSummarizerProvenanceError`, a `ValueError` subclass) rather than a separate validation pass a caller could skip — the invalid state cannot be constructed at all. Tested: missing-both raises; both-present succeeds; a non-ai-summary `access_apparatus` does NOT require them (so the guard doesn't over-reject the common case). |
| P4.3 | Append-only `EventRecord` log | §51 | attempting to mutate/delete a past event raises; replay reconstructs a sequence of study states | **done** | `EventRecord` dataclass (fields match spec §51 exactly) + `append_event()`/`read_events()` against `events/events.jsonl` (the file `workspace.new_study()` already creates empty, P1.7). No update-in-place function exists; `mutate_event()`/`delete_event()` are present specifically as functions that always raise `EventLogMutationError`, so a caller reaching for "the obvious API" gets a clear, documented refusal rather than a missing-attribute crash or, worse, silent success. `read_events()` returns the append-ordered sequence — "replay" at v0.1 IS reading this list in order, since no separate derived-state machine exists yet (stated in the docstring, not silently implied to be more than that). Tested: 3 chained events (via `parent_event_ids`) round-trip in append order; both mutate/delete calls raise. |
| P4.4 | `ResearchRelease` generator incl. `data_license_notice` | §55, §56 (v2.3.0) | release refuses to generate if `data_license_notice` is empty; generated release for the fixture study contains the three-part licensing chain text verbatim | **done** | `ontograph/release.py`: `DATA_LICENSE_NOTICE` constant holds spec §55's licensing-chain sentence verbatim (public-domain poem texts / Ganjoor's own compilation-and-summary work under its attribution convention / MIT fork-generated summaries) rather than a paraphrase. `generate_release()` raises `MissingLicenseNoticeError` for an empty or whitespace-only notice; on success writes `releases/vX.Y.Z/release.json` and a human-readable `RELEASE.md` that both carry the notice. Tested: empty and blank-string rejection; generated release's notice matches the constant exactly, in both the dataclass and the two written files. |
| P4.5 | Release-as-git-tag convention | §60 (v2.3.0) | `ontograph release` creates a git tag in the study workspace matching the release version | **done** | `release_as_git_tag()` commits the release files `generate_release()` wrote and tags that commit `vX.Y.Z`, reusing `workspace.py`'s existing `_run_git()` helper rather than re-implementing git plumbing. Tested: tag appears in `git tag --list`; the tag's commit SHA equals `HEAD` after the call. Full suite: 73 passed. |

## Phase 5 — CLI

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P5.1 | `ontograph` console script wired to Phases 1–4, every verb accepts `--json` | §62, §78 | each of `study new`, `field build`, `object add`, `calibrate`, `census`, `map recurrence`, `companions`, `compare`, `ablate`, `release` runs against the fixture and exits 0 with well-formed JSON | **done** | `ontograph/cli.py`: argparse-based, nested subparsers matching spec §62's two-word verbs exactly (`study new`, `field build`, `object add`, `map recurrence`); every verb function returns a plain dict, `main()` prints it as JSON (compact by default, pretty with `--json`) and returns an int exit code, so the `pip install -e .` console-script entry point (`sys.exit(main())`) works unmodified. `tests/test_cli.py::test_full_verb_sequence_against_fixture` runs all ten verbs in sequence against the fixture (study new → field build → object add ×2 → calibrate → census → map recurrence → companions → compare → ablate → release) and checks specific fixture numbers at each step (e.g. census hit_count=7, ablate remaining_poem_scale=1), not just exit code 0. Verified the actual installed console script (`ontograph --help`) too, not just `main()` called in-process. **Honest scope limit stated in the module docstring**: no `assess` verb exists yet to build an `OccurrencePolicy` from a study's own Occurrence Ledger, so `map`/`companions`/`ablate`/`compare` all operate at the anchor level and say `"mode": "anchor"` explicitly in their output — never silently presented as assessed-level results (the same Finding-1 discipline as P3.3-P3.9, now applied to the CLI surface). `field build`'s `--category` and `compare`/`ablate`'s field/remove specs are restricted to what `ScopeSpec`/`scan_corpus` can actually resolve today (`poet:<slug>` only) — an unsupported spec raises `CLIError`, it is not silently ignored. |
| P5.2 | Verb-naming check: no `search`/`query` verbs anywhere in the CLI | §25 (v2.3.0 disambiguation note) | `ontograph --help` output contains neither literal string | **done** | `test_help_contains_no_search_or_query_verb` asserts both literal substrings are absent from `--help` output — catching not just verb names but any description text (initially failed on the word "research" in the CLI's own top-level description, since it contains "search" as a substring; reworded to "inquiry engine" once this was caught, a real instance of the test doing its job rather than a formality). |
| P5.3 | Explicit failure mode: non-zero exit + stderr message on bad input, on missing workspace, on malformed field spec — never a silent empty JSON success | §61, §78 | one negative test per failure class above | **done** | Bad input (missing required flag) is argparse's own built-in behaviour, exercised directly (`SystemExit` with non-zero code, stderr usage message). Missing workspace and malformed field spec (`--category`, unsupported `--remove`/`--rerun`/`--field` prefixes) are a new `CLIError` exception `main()` catches, printing to stderr and returning 1 — `test_missing_workspace_exits_nonzero_with_stderr_message` and `test_malformed_field_spec_category_rejected` both additionally assert stdout is empty on failure, the literal "never a silent empty JSON success" requirement. Full suite: 78 passed. |

## Phase 6 — Claude Code binding

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P6.1 | Point `.claude/skills/persian-poetry-ontograph/SKILL.md`'s invocation examples at the real, now-working CLI (already scaffolded this session; update only the "Status: scaffold" note once Phase 5 is done) | Part XIII | manually run one invocation from the SKILL.md example block against the fixture, confirm the JSON shape matches | **done** | Ran the full corrected invocation block (`study new` → `field build` → `object add` ×2 → `calibrate` → `census` → `map recurrence` → `companions` → `compare` → `ablate` → `release`) against the fixture by hand; all ten exited 0 with well-formed JSON matching the fixture's own ground truth (e.g. companions poem_scale=[9101,9102,9106,9201], ablate remaining_poem_scale=1). **Two real mismatches found and fixed while doing this, not just a copy-paste update**: (1) the original example's `object add` line for mirror omitted `--address mirror`, so the object address defaulted to the Persian label itself rather than the ASCII slug every later `--object mirror` call assumed — fixed by adding `--address` explicitly and documenting what it's for; (2) the example used `--category ghazal` and a placeholder `--field A --field B`, neither of which the real P5.1 CLI accepts (`--category` raises `CLIError`; `--field` requires a `poet:<slug>` prefix) — rewritten to what the CLI actually supports, with the current restriction stated in a new "Status" scope note rather than left to surprise a researcher at runtime. |
| P6.2 | `.claude/settings.json` allowlist entries for corpus-mutating/release verbs per §79 | §79 | settings.json contains explicit entries for `field build`, `object add`, `calibrate`, `release`; no blanket Bash grant added | **done** | New `.claude/settings.json` with `permissions.allow` entries for all ten non-destructive verbs (`Bash(ontograph <verb>:*)` each), including the four §79 names explicitly. No blanket `Bash(*)`/`Bash(ontograph:*)` entry exists — every rule names a specific verb. Workspace-deletion and forced-object-merge (neither of which the v0.1 CLI even implements yet) are correctly absent, per §79's "destructive verbs ... stay outside any allowlist." `tests/test_settings_allowlist.py`: file parses, the four required verbs are present, and a blanket-grant check fails the test if one is ever added. Full suite: 81 passed. Phase 6 (P6.1-P6.2) complete. |

## Phase 7 — Tests and gates (v0.1 completion)

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P7.1 | Deterministic corpus test suite (full §65 list) | §65 | `pytest tests/deterministic/ -q` all pass | **done, 14 checked + 5 honestly skipped** | `tests/deterministic/test_appendix_65.py`, one test per §65 bullet in spec order. 14 bullets have a real, passing assertion — several are genuinely new checks, not restatements of existing coverage: source-file count vs. manifest, field-membership reproducibility across independent scans, an Anchor Hit count cross-checked against an *independent* direct-source scan (a separate regex-split, not the engine's own tokenizer — real gap found: this cross-check didn't exist anywhere before this row), mixed-script tokenization, and **stratified calibration sampling with `strata_key`, which had zero test coverage anywhere in the codebase before this row** despite being implemented since P2.1. 5 bullets are `pytest.mark.skip`-marked with the specific reason and, where applicable, the ledger row that scoped the capability out: category subtree expansion (P1.3), pairwise-mapping estimated-mode blocking (no `MODE_ESTIMATED` in `compare.py`), mediation formulas (v0.2, spec §72), Mapping Object source-return (P4.1's scope excludes `MappingObject`/`OperationSpec`/`ReductionRecord`), corpus/QMD synchronization guard (no retrieval channel integrated in v0.1). `pytest tests/deterministic/ -q` reports 14 passed, 5 skipped — skips are visible in the report, not silently absent from the file. |
| P7.2 | Epistemic contract test suite (full §66 list — the "does NOT" scenarios) | §66 | `pytest tests/epistemic/ -q` all pass | **done, 9 checked + 5 honestly skipped** | `tests/epistemic/test_appendix_66.py`, one test per §66 scenario. 9 real assertions, including two genuinely new checks beyond what Phase 3/4's own tests already covered: `test_profiles_are_never_overwritten_only_appended` (writes two `ProfileRecord`s for the same object under different conditions to the same workspace and asserts both survive in order — no prior test exercised `records.py`'s append-only guarantee for `ProfileRecord` specifically) and `test_rejected_traces_are_never_deleted` (asserts `ontograph.records` exposes no `delete_record` function at all, then round-trips a `status="residue"` `TraceRecord` and confirms it's still there). 5 scenarios `pytest.mark.skip`-marked, each naming the record type or module that would need to exist first: candidate-anchor promotion review gate, category→genre mapping declaration, graph centrality merging (§33, not built), `ClaimRecord`-from-AI-proposal guard (`ClaimRecord` itself is out of P4.1's scope, not just its guard), and the same QMD-sync gap as P7.1. `pytest tests/epistemic/ -q` reports 9 passed, 5 skipped. |
| P7.3 | Close–distant loop tests A–F against the fixture | §67 | `pytest tests/loop/ -q` all pass; Test F may be `xfail`-marked with a comment pointing at §72 (mediation is v0.2 scope) rather than silently skipped | **done** | `tests/loop/test_close_distant_loop.py`. The mini-ganjoor fixture was purpose-built for exactly these six pressure cases, so all six reuse it directly rather than needing new corpora: **A** (concentrated frequency) — mirror's 4-of-5-accepted-hits-in-one-poet shape, asserting `spread`'s "2 poets touched" alone would read as misleadingly broad unless `concentration`/`gries_dp` are consulted alongside it. **B** (scale collapse) — `relation_scale_profile` on mirror×rust: poem-scale 4, couplet-scale 3, poem 9102 isolated as the poem-only survivor, with both classes (9102 and 9101) confirmed independently source-returnable via `open_context_ladder`. **C** (lexical ambiguity) — poems 9105/9106 (the fixture's deliberate ambiguous/rejected cases) are still raw anchor matches but excluded once `accepted_poem_ids` reflects the researcher's assessment, directly modelling "narrow before field-wide interpretation." **D** (ablation false centre) — reuses `ablation_retention` on the sample1 removal case (P3.8). **E** (local close-reading insight) — writes a `TraceRecord` describing a one-poem material pairing, separately runs a field-wide `typed_coincidence` recurrence test, and asserts the stored Trace is byte-for-byte unchanged by having run it — the first test in the suite that checks a record survives a read-only analytical operation elsewhere in the engine. **F** (research-made mediation) is `xfail(strict=True)`, importing `ontograph.mediation.mediation_incidence` (which does not exist) rather than being silently skipped — `strict=True` means this row would itself start failing the moment mediation is implemented, forcing this test to be rewritten for real rather than forgotten. Full suite: 109 passed, 10 skipped, 1 xfailed. |
| P7.4 | Implementation gates 1–5 | §69 | a single `ontograph validate --gates --json` (or equivalent script) reports all five gates green against the fixture, AND re-runs P3.9's invariant audit as part of gate 3 (deterministic-engine gate) — a gate report that doesn't re-check Appendix A live does not count as green | **done** | `ontograph/validate.py`'s `run_gates()` + `ontograph validate --gates --json` CLI verb, both checked in `tests/test_validate.py`. Gate 1 (corpus pin) records the corpus root's real `git rev-parse HEAD` alongside the manifest hash — not a hand-supplied fake commit — and reports the pin as unverifiable rather than pinned if the root isn't a git checkout at all. Gate 2 (lexical/object separation) is a structural check that `AnchorHit` carries no decision/acceptance field. **Gate 3 re-runs P3.9's Appendix A invariant audit live via a real `pytest` subprocess** (`tests/invariants/test_appendix_a.py` plus the field/anchors/compare/ablation fixture suites) against the current source tree, not a cached "it passed once" result. Gate 4 confirms the CLI is actually invocable in-process; **a real bug was found and fixed while building this**: gate 4's own `--help` check printed argparse's help text straight to the process's real stdout even though it ran in-process, which would have corrupted `ontograph validate --gates --json`'s own JSON output the moment gate 4 ran as a step inside it — fixed with `contextlib.redirect_stdout` around that one call, and `test_validate_cli_verb_reports_all_green_json` asserts the full CLI output still parses as clean JSON specifically to catch a regression of this. Gate 5 is P7.6's replay, reused directly. All five gates green against the fixture; `all_green: true`. |
| P7.5 | Occurrence-assessment scalability gate | §70 | the fixture study demonstrates all three routes on objects large enough to actually pressure them: `assessed-full` on mirror's 7 hits, `assessed-rule` with one versioned rule (validated against the mirror/rust divergence case, poem 9106, so the rule can be shown to correctly exclude it), and `estimated` on the **HEART object at a genuine <100% sampling fraction** (per P2.4) with its Wilson interval verified to bracket the true 11/27 prevalence — never mislabeling one route as another | **done** | **Route 2 (validated rule) did not exist anywhere in the codebase before this row and was built here, not just tested** — `census.py` gained `apply_occurrence_rule()` (a versioned, deterministic `figurative-context-stoplist-v1` rule: rejects a mirror hit whose verse contains "دل"/"خاطره", the two abstract nouns that turned out, on the fixture's own human review, to mark the figurative "mirror of the heart"/"mirror of memory" readings) and `validate_rule_against_reviewed_material()`. Calibrated against all 7 reviewed mirror hits: **7/7 agreement**, including the specific required demonstration that it correctly excludes poem 9106 — checked before assuming so, not asserted on faith (`tests/test_occurrence_rule.py`). The rule's real limits are stated in its own docstring, not hidden: binary only (no ambiguous bucket), verse-local lexical co-occurrence only, calibrated on one object over one fixture with no generalization claim. `tests/test_scalability_gate.py` demonstrates all three routes together, each under its own explicit route label (`assessed-full`/`assessed-rule`/`estimated`), plus a dedicated `test_routes_are_never_mislabelled_as_each_other` checking the three result dataclasses (`Prevalence`/`RuleValidationReport`/`EstimatedIncidence`) share no field that would let one be mistaken for another (e.g. no `wilson_lo` on the rule report). Route 3 reuses P2.4's HEART-object estimator at a genuine <100% sampling fraction (15/27), Wilson interval verified to bracket the true 11/27. Full suite: 120 passed, 10 skipped, 1 xfailed. **Phase 7 (P7.1-P7.6) is now complete — this reaches the v0.1 stop condition per `BUILD_PLAN.md`'s Definition of Done, on the fixture.** |
| P7.6 | End-to-end replay: run one full study (Field Charter → Release) on the fixture, then independently reconstruct its state from the release package alone | §69 gate 5 | a second script rebuilds study state from `release.json` only and asserts it matches the live workspace | **done** | `tests/test_release_reconstruction.py::test_end_to_end_replay_field_charter_to_release_and_back`. Runs the full chain against the live fixture (Field Charter over all 27 poems → mirror census, 7 hits → `ProfileRecord`/`FindingRecord` written into the live workspace's `research/*.jsonl` → `generate_release()` → `release_as_git_tag`), then calls `reconstruct_from_release()` reading **only** `release.json` and asserts its output equals a fresh, independent `read_records(ws, ...)` off the live workspace — not just values already sitting in local variables from step 1. **A real spec-schema gap was surfaced and resolved while building this, not glossed over**: spec §55's `ResearchRelease` names `object_addresses`/`load_bearing_profiles`/`findings` as fields but never fixes what their list elements contain, and Object Address/Lexical Anchor records aren't formal JSONL CRUD in this build (P4.1's scope was Trace/Relation-Object/Profile/Experiment/Finding only) — so a literal reading of the schema would make "reconstruct from release.json alone" impossible without re-reading the live workspace, defeating the gate. Resolved by a documented v0.1 embedding convention (`release.py`'s own docstring): `object_addresses` entries are `{"id":.., "anchors":[...]}` dicts and `load_bearing_profiles`/`findings` entries are full serialized records, not bare id strings — a concrete choice within what the spec schema actually specifies, stated as a choice rather than picked silently. This row, together with P7.1-P7.5, reaches the v0.1 stop condition (`BUILD_PLAN.md` Definition of Done) once P7.5 also passes. |

## Phase 8 — Real-corpus integration (manual, not loop-driven)

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P8.1 | Point pinned-corpus config at the real corpus (this repo's own root — `poets/`, `index/`, `manifest.json`, commit `500a82a55f90870f2214f39a8069182dbad4f183`), rebuild real SQLite index | §56 | poet/poem counts (234 poets, 132,591 poems) match `manifest.json` at repo root, which matches the live manifest re-audited in `EVALUATION.md` | **done — mechanical part fully run, including 2 real bugs found and fixed** | **Poets match exactly: 234.** Poems: `scan_corpus` finds **132,538** actual poem JSON files against the manifest's declared **132,591** — fully reconciled, not left as a mystery: every `poets/*/**/_cat.json`'s own `Poems` list sums to exactly 132,538, and every one of those declared poem IDs matches an actual file 1:1 (zero missing, zero orphaned, verified by set difference on IDs, not just counts). So **`manifest.json`'s top-level `PoemsCount` is simply stale relative to the corpus's own internally-consistent category indices** — a real corpus-pin/gate-1 concern (the root's own summary metadata drifted from its own per-category truth), not a missing-data problem. (One local red herring resolved along the way: `poets/osmani/qushayriyya/bab3/_cat.json` declares 6 poems whose files turned out to live under a differently-slugged sibling folder `bab-svm` for the same logical category — a folder-naming quirk, not missing content.) **SQLite index successfully rebuilt against the full real corpus** (`corpus.build_index`, 240.6s): 234 poets, 132,538 poems, 669,377 sections, 3,052,430 verses, 1,567,488 couplets, 3,052,430 normalized verses, 25,778,468 token offsets — all matching `scan_corpus`'s own 132,538, confirming the engine's count is the internally-consistent one. **Two real crash bugs found and fixed to get this far, both real-corpus edge cases the fixture never exercised**: (1) `anchors.census()`/`corpus.build_index()`/`census.open_context_ladder()` all indexed `verse["CoupletIndex"]` by bracket access, which raised `KeyError` on the ~647 real poems containing `Position: "Comment"` prose-commentary verses (Sufi treatises like Osmani's Qushayriyya, Araqi's Lama'at) that have no `CoupletIndex` at all — fixed via `.get()` throughout, `AnchorHit.couplet_index` is now `int \| None`, and `compare.py`'s `_couplets()` explicitly excludes `None` from couplet-scale co-incidence (two unrelated "no couplet" hits are not the same couplet). (2) `corpus.py`'s `sections` table assumed `(poem_id, idx)` was a valid primary key; real poem 142187 has a `WholePoem` section and a `Couplet` section both at `Index: 2`, and even a widened `(poem_id, idx, section_type)` key still collided 3,586 times across the real corpus — `Section.Index` is not a reliable per-poem identifier for any `PoemFormat`, extending P0.5's `SectionIndex1` finding; fixed by dropping the primary key on `sections` entirely (kept as a plain, non-uniquely-keyed table, addressed by `poem_id`). Both fixes have dedicated regression tests (`tests/test_comment_verse_real_corpus_case.py`, `tests/test_index.py::test_build_index_survives_a_poem_with_duplicate_section_index_across_types`) reproducing the exact real shapes without needing the real corpus in CI. The 2.1GB built index itself is not committed (local/rebuildable derived artifact, per spec §60's own "large derived tables ... referenced by hash instead of duplicated into every release"). |
| P8.2 | Run the spec's own worked example (§45, mirror/rust in Hafez) as a smoke test on the real corpus | §45 | a human reviews the actual output against §45's narrative | **run in full (both the literal Hafez scope and the whole real corpus) — human literary review is the one part that genuinely still needs a human** | `implementation/p8_2_worked_example_smoke_test.py` runs two scopes. **Part 1, Hafez only (as this row literally asks, 695 poems)**: mirror 21 hits/21 poems, **rust 0 hits** — the exact co-incidence §45 narrates does not occur in Hafez under `زنگار` alone. A 6-hit calibration sample (seed 20260830) surfaced a genuine calibration case unprompted: poem 2435's hit sits inside the fixed idiom "هر آینه" ("indeed/certainly," not a literal mirror) — exactly what Occurrence Assessment exists to catch. **Part 2, whole real corpus (132,538 poems, 234 poets), run because Part 1 alone can't exercise §45's full narrative**: poem-scale co-incidence **378 poems**, couplet-scale **310 couplet-cases**, across **84 distinct poets** — a real, substantial relation, not a fixture artifact. Dominant poet: Saeb Tabrizi (`saeb`, 149/378 poems, 140/310 couplets). **Ran the ablation step §45 itself calls for** ("does it survive removal of the dominant poet?"): removing Saeb's 9,877 poems retains **60.6% poem-scale / 54.8% couplet-scale** — a majority, so per §45's own branching this reads as a relation that *survives* rather than one collapsing to a false centre, an honest empirical result either way. One concrete couplet-scale example surfaced: poet `moulavi` (Rumi), poem 3277 couplet 6 — "آیینه که شمس الحق تبریز بسازد / زنگار کجا گیرد و صیقل به چه باید" ("The mirror that Shams al-Haqq of Tabriz makes — how could rust ever touch it, what need for polishing?"), a genuine literal-to-figurative mirror/rust/purification pairing in the actual mystical register §45's own narrative imagines. **What remains open, and genuinely requires a human**: reading these results against §45's prose (is the couplet example's figurative reading sound; does 55-60% retention actually support "the relation deserves a persistent Relation-Object" or does it call for a closer look at Saeb's outsized share first) — that literary/methodological judgment is not something re-running this script again would produce. |
| P8.3 | Follow-up correction to P5.1/P6.1/P6.2 (not a rewrite of their history, per this ledger's own rule): add the `assess` CLI verb and `--mode anchor\|assessed` to `census`/`map recurrence`/`companions`/`ablate`/`compare` | §8.1, §60, Appendix A | round-trip `assess` + `--mode assessed` against the fixture matches the exact numbers P2.2/P2.3/P3.3/P3.8 already established at the Python-API level | **done** | Testing P8.2 against the real corpus surfaced that the CLI itself had no way to actually record Occurrence Assessments — `census.py`/`compare.py`/`ablation.py` have supported assessed mode since Phase 2/3, but P5.1 never exposed it, so a real researcher using only the CLI could not get past anchor-level results. New `ontograph assess <study> --object <addr> --poem-id <id> --decision accepted\|rejected\|ambiguous` appends one `OccurrenceAssessment` to `corpus/occurrence-ledger.jsonl` (spec §60's own workspace layout, not a new ad hoc path). `census`/`map recurrence`/`companions`/`ablate`/`compare` all gained `--mode anchor\|assessed` (default `anchor`, unchanged from before); `--mode assessed` loads that ledger and raises `CLIError` — never silently falls back to anchor — if no assessments are recorded yet for an object involved, the same Finding-1 discipline `compare.py`/`ablation.py` already enforce at the Python level, now enforced at the CLI surface too. `tests/test_cli.py::test_assess_and_mode_assessed_matches_fixture_ground_truth` replays every entry from `canonical-study-assessments.json` through the real `assess` verb, then checks `census --mode assessed` (5/27), `companions --mode assessed` (poem-scale [9101,9102,9201], the Finding-1 divergence), and `ablate --mode assessed` (1/3, 1/2 retention) against the exact numbers already established — a second, independent regression guard on top of P3.3/P3.8's own tests. `.claude/skills/persian-poetry-ontograph/SKILL.md` and `.claude/settings.json` updated to match (the skill's invocation example now shows an `assess` call before any `--mode assessed` use; `assess` and `validate` — the latter also missing from the allowlist since P7.4 — both added to `permissions.allow`). 134 tests passing, 10 skipped, 1 xfailed. |
| P8.4 | Follow-up correction to P1.4 (found via the user's own real-corpus test session, not the automated loop): `scan_poets()` read the wrong real-corpus field names | §11, §23.2 | `scan_poets()` against the real corpus root returns real birth/death years for all 234 poets, not `None` for all of them | **done** | The vendored real corpus's `poet.json` uses `BirthYearInLHijri`/`DeathYearInLHijri` (plus `ValidBirthDate`/`ValidDeathDate` booleans) — P1.4's fixture invented `BirthYearLunarHijri`/`DeathYearLunarHijri` instead, a naming error that P0.5's real-schema spot-check never caught because it checked verse/couplet structure, not poet metadata. Against the real corpus this silently returned `None` for every poet's dates — the poet-life chronological proxy (§11/§23.2) was completely broken there, with no error at all, and no automated test caught it because every test ran against the (wrong-named) fixture. Fixed `field.py`'s `scan_poets()` to read the real field names; added `valid_birth_date`/`valid_death_date` to `PoetRecord` (exposed, not yet factored into `poet_life_overlaps_hijri_range()`'s matching logic — a methodological call left open, not silently decided); fixed the fixture's 4 `poet.json` files and `generate_fixture.py` to use the real field names plus `ValidBirthDate`/`ValidDeathDate: true`, rather than leaving the fixture's own naming error in place. Verified against the real corpus: `scan_poets(real_root)` now returns real dates for 234/234 poets (was 0/234). 134 tests passing, 10 skipped, 1 xfailed — unchanged, since the fixture's values didn't change, only its field names. |

## Phase 9 — Usability & methodology hardening (guided, not loop-driven)

**Do not drive this phase with `/loop /ontograph-build`.** That skill's own
stop condition re-verifies Phase 0–7 and declares v0.1 complete the moment
it finds no `todo` row above Phase 8 — which is already true, so it would
stop immediately without ever looking at these rows. Phase 9 rows are meant
to be worked through directly, with the user present, the same way Phase 8
was — not because they're less important, but because P9.5 in particular is
a real UX design decision, not a mechanical spec-to-code translation, and
the user has asked to review the result before another round of their own
testing.

**Origin:** the user tested the finished v0.1 apparatus themselves (a
9-turn session, transcript reviewed in full) and found real friction beyond
what Phase 0–7's own tests could catch, since those tests all run against
the fixture. Full diagnosis is in this session's own conversation history;
the load-bearing findings are repeated in each row's Notes below rather
than assumed to be remembered. Two design decisions the user made explicit,
binding on P9.5 and P9.8 respectively:
- **P9.5 (calibration/assessment UX):** "this is the art of designing the
  experience... guidance for options available show the possible
  continuance ways, but not bounding the possibilities may occur with
  agency of the researcher user." The flow surfaces what's possible next
  at each step; it never forces one path or hides a legitimate move
  (accept/reject/ambiguous, narrow the anchor, split the object, widen or
  stop the sample, promote to Trace) — this is the same "next-action
  choices" principle `persian-poetry-ontograph/SKILL.md`'s own Progressive
  Disclosure section already names in the abstract; P9.5/P9.6 make it
  concrete at the one place (calibration) the user's test session skipped
  it entirely.
- **P9.8 (output format):** artifact/HTML plus a Markdown report, by
  default, unless the researcher asks for something else.

Two dependency chains, in order — do not build B on top of an unfinished A,
each phase's tests must re-run the *whole* existing suite (currently 134
passed/10 skipped/1 xfailed) plus a real-corpus check, since this touches
shared code (`field.py`, `cli.py`, `census.py`) many existing tests depend
on:

### Chain A — infrastructure (study-bound corpus, cached index, real field scope)

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P9.1 | `ontograph study new` accepts and persists `--corpus-root` in `study.yml`; every other verb reads it from there when `--corpus-root` is omitted (an explicit flag still overrides) | §60, §62 | fixture-level test: `study new --corpus-root X`, then `census` with no `--corpus-root` flag succeeds and matches the same result as passing it explicitly | **done** | User's test session: the agent guessed `study init --corpus-root` (wrong subcommand), then `study new --corpus-root` (wrong verb — it's a `field build`/`census`/etc. flag), and only got it right after reading `cli.py`'s source. A study not remembering its own corpus is real, repeated friction, not a one-off mis-guess. **Implementation (resumed an interrupted session's uncommitted working-tree diff, which was correct as far as it went but incomplete):** `workspace.new_study()` gained `corpus_root=None` and persists it into `study.yml` (`yaml.safe_dump`, unicode-safe); `workspace.read_study_config()` reads it back tolerantly (`{}` for missing/empty file — no stored root is a normal case, not corruption); `cli._resolve_corpus_root(args, ws)` implements the precedence rule (explicit flag → stored root → clean `CLIError`, never silent). **The incomplete half found and finished here:** the interrupted diff wired `_study_new` to `args.corpus_root` but never added the argument to the `study new` parser (every `study new` call raised `AttributeError`, 4 existing tests red) — `--corpus-root` added there. **The actual point of the row:** `with_corpus`'s `--corpus-root` changed from `required=True` to `required=False, default=None`, so all 8 parser-level corpus verbs (`field build`, `calibrate`, `census`, `map recurrence`, `companions`, `ablate`, `compare`, `validate`) accept its absence; the 7 scan-consuming ones go through the resolver (`validate` passes `None` straight to `run_gates`, unchanged semantics). Honest scope note: the plan also expected `assess` to have "gained the resolver", but `assess` never consumes a corpus at all — there is nothing to resolve — so what was verified instead is that `assess` runs cleanly with neither a flag nor a stored root (asserted in its test). TDD: the 3 new P9.1 tests were written first and watched failing (AttributeError on the unfinished half), then passed after the two parser fixes; the override test stores a deliberately bogus root and proves the explicit flag still wins (hit_count 7 from the real fixture, not 0 from the bogus path). Full suite: **137 passed, 10 skipped, 1 xfailed** (134 + 3 new; the 10 skips / 1 strict-xfail unchanged). |
| P9.2 | Corpus-root-keyed SQLite index cache (build via the already-correct `corpus.build_index()`, store keyed by corpus root + a real content signal — not just mtime — reuse across CLI invocations instead of rebuilding) | §57, §62 | a warm-cache `census` call against the real corpus is measurably faster (order of seconds, not the ~60–90s cold path) on a second invocation with the same corpus root; a cache-invalidation test proves a changed corpus root doesn't serve stale results | **done** | **Implementation (this row's verify run against the real corpus):** new `src/ontograph/index_cache.py`; cache entry = SQLite file + meta sidecar under a deterministic path keyed by SHA-256 of a composite content signal (resolved root + manifest SHA-256 + poem file count + cheap per-shard fingerprint = hash over each poet-dir's (relpath, size, mtime_ns) — **never mtime alone**). Warm path recomputes the signal and compares against the meta's stored signal; any mismatch (edited poem, changed manifest, renamed shard, different root, corrupt meta) misses and rebuilds via the proven `corpus.build_index()`. Cached DB opened read-only (`file:...?mode=ro`). 8 new tests in `tests/test_index_cache.py`, all invalidation cases against fixture COPIES (incl. edited-poem, changed-manifest, renamed-shard, corrupt-meta). `records_from_index`/`census_from_index` added here (P9.3's substrate) and tested order-identical to `scan_corpus`/`anchors.census` (calibrate is seed-position-sensitive). **Real corpus (132,538 poems / 234 poets):** cold build via `build_index()` = 7,559s wall (vs the ~240s P8-era assumption — see I/O note below); build manifest 3,052,430 verses / 25,778,468 token rows; warm reopen = cache hit (46.5s, dominated by the content-signal stat pass — halved afterwards by computing the signal once); warm `census_from_index` = 13,444 hits (mirror 12,589 across 8,792 poems; rust 855 across 788) **byte-identical to the cold-path census** (asserted in-process). **Real I/O finding for future planning:** today's machine reads corpus JSONs at ~26–38 files/s cold (≈40ms/file, near-zero CPU — filter-driver/AV latency, machine-wide, also on `.venv` files; parallel readers don't help, ~37/s aggregate) vs the P8.2-era ~1,500+/s; warm re-reads are ~0.75ms/file and persist (RAM 23.7GiB, 10.5 free). The 240s assumption is not reproducible under today's I/O; the cold build is paid exactly once and every later invocation is warm. | Depends on P9.1 (natural place to key the cache). User's test session: `field build` over the real corpus took 1m21s; `census` for one word timed out at 2 minutes and had to be backgrounded with manual `ps`/`kill` babysitting. Every CLI call currently does a full `scan_corpus()` + full re-parse/re-tokenize from scratch — `corpus.build_index()` (built in Phase 1, proven working against the real corpus in P8.1) is never used by the CLI at all. This is the single biggest real-usability gap found. |
| P9.3 | `census`/`calibrate`/`map recurrence`/`companions`/`ablate`/`compare` rewired to query the cached index (P9.2) instead of `scan_corpus()`+`census()` from scratch each call | §57, §62 | full existing suite still passes unmodified (fixture-level numbers must not change, only the code path computing them); a timing assertion on the real corpus | **done** | **Implementation:** all 7 scan-consuming CLI verbs (`field build`, `calibrate`, `census`, `map recurrence`, `companions`, `ablate`, `compare`) now go through `cli._open_cached_index()` → `index_cache.get_or_build_index()` (P9.2) → `records_from_index` + `census_from_index`; `scan_corpus` is no longer called anywhere in `cli.py` (import removed). The read-only connection is closed in `finally` on every verb (Windows file-locking). **Fixture numbers unchanged at all:** the entire existing suite passes unmodified — every ground-truth assert (census hit_count 7; map distinct_poems 7; companions poem_scale [9101, 9102, 9106, 9201]; assessed 5/27 with ambiguous_only 1; assessed companions [9101, 9102, 9201] i.e. the 9106 anchor-vs-assessed canary intact; assessed ablation 1/3 & 1/2) — because `census_from_index` is token-exact and order-identical to `anchors.census` (tested explicitly in `tests/test_index_cache.py::test_index_backed_census_matches_scan_census_exactly`), so even seed-position-sensitive `calibrate` samples are unchanged. **Real-corpus timing:** warm `records_from_index` 132,538 poems in 7.3s; warm `census_from_index` (mirror+rust, whole corpus) 13,444 hits in 10.5s — seconds, not the 60–90s scan + 2-minute-census of the P9.2 row's own problem statement; warm cache reopen confirmed a hit (no rebuild). Full suite after the rewiring: **145 passed, 10 skipped, 1 xfailed** (before P9.2's own 8 tests landed; 147 after — the rewiring itself added nothing, changed nothing). | Depends on P9.2. |
| P9.4 | `field build`'s stored scope (`field/scope.json`) becomes a real filter other verbs read and intersect with their own query, instead of being written and never read again | §7, §23, §48 | fixture-level regression test: build a field scoped to one poet, then `census` on an object present in another poet's poems returns 0 hits within that study, even though the object exists corpus-wide | **done** | **Implementation:** `field.py` gained `scope_from_dict()` (reconstructs a ScopeSpec from the exact `dataclasses.asdict` JSON `field build` writes; unknown kinds raise, never silently resolve to nothing — roundtrip tested for every kind incl. union/intersect/difference/none). `cli._scope_allowed()` reads `field/scope.json` when present and the 6 query verbs (`calibrate`, `census`, `map recurrence`, `companions`, `ablate`, `compare`) intersect their hits AND their populations with it: assessed-mode denominators, spread totals, companions' lift field_size, ablation's removed-poem set, and compare's field poem sets are all the scoped ones — scoping filters via the cached index's records (a SELECT, not a re-scan), per the row's own note. No `scope.json` → no filtering (studies that never built a field, and P9.1's stored-corpus flow, unaffected — asserted in the regression test which stores the root and omits all flags). **Regression test** (`tests/test_cli.py::test_p94_scope_filters_census_to_the_stored_field`): field scoped to `sample1`; object `garden` (باغ — token present ONLY in sample2's poem 9202) → `census` in the scoped study returns **0 hits / 0 poems**, while an unscoped study returns >0 hits incl. 9202; re-running the unscoped study with an explicit all-poems field build gives **identical numbers** (scope=all is a no-op). Full suite: **148 passed, 10 skipped, 1 xfailed**. | Depends on P9.3 (scoping should filter via the index, not re-scan-then-filter). User's test session never hit this directly, but it's a direct architecture gap found while reading `cli.py`: "Construct the Object-Field," the spec's own first methodological step (§7), currently does nothing to any later operation. |
| P9.5-A | Chain-A closeout: full existing test suite + new P9.1–P9.4 tests all green; real-corpus smoke re-run (`p8_2_worked_example_smoke_test.py` or equivalent) confirms materially faster wall time than the P8.2 run it's compared against | — | one command showing full suite green + one timed real-corpus run with numbers matching P8.2's original findings, faster | **done** | **Closeout evidence (one session, both commands):** (1) full suite **148 passed, 10 skipped, 1 xfailed** — the P8.2-era floor (134/10/1) plus every P9.1–P9.4 test, all green. (2) `implementation/p9_5a_timed_smoke_test.py` — the P8.2 worked example (spec §45, mirror/rust) re-run through the cached pipeline against the real corpus (132,538 poems / 234 poets): warm open `cache_hit=True` 6.5s, `records_from_index` 2.2s, warm census 13,444 hits in 3.3s — **12.0s total wall time**, vs P8.2's recorded `field build` 1m21s and the 2-minute single-word census timeout (and the old pipeline would pay the scan again on every verb). **Numbers match P8.2's original findings exactly:** poem-scale co-incidence **378**, couplet-scale **310**, **84** distinct poets, dominant poet `saeb` (149/378), ablation removing saeb's 9,877 poems retains **60.6% poem-scale / 54.8% couplet-scale** — identical to the ledger's P8.2 record, computed from the cached index. **Standing caveat (per plan risk #2, recorded in P9.2's notes):** today's machine file-I/O is ~40x slower cold than the P8.2 era, so the one-time cold build measured 7,559s, not ~240s — the comparison here is warm-path vs P8.2's cold-path numbers, which is the conservative direction (warm-vs-warm would be even faster). **Chain A is complete; stopping here per the row's own instruction — P9.5 (Chain B, guided calibration UX design) requires the user present before any design is finalized.** | Depends on P9.1–P9.4. This is the checkpoint before starting Chain B — do not start P9.5 (calibration UX) on a slow, unscoped foundation. |

### Chain B — methodology (guided calibration, presentable Findings)

| ID | Task | Spec refs | Verify | Status | Notes |
|---|---|---|---|---|---|
| P9.5 | Design and build a guided calibration/assessment flow: pull a properly-sized sample (spec default ~30, adjustable) via `calibrate`, walk the researcher through each hit with the context ladder, and surface — never force — the next-action choices named above (accept/reject/ambiguous, narrow anchor, split object, widen/stop sample, promote to Trace), batching the resulting `assess` calls rather than requiring one CLI invocation per hit | §9, §27.2, §28.4 | a scripted fixture-level walkthrough (mirror's 7 hits) reaches the same accepted/rejected/ambiguous split as `canonical-study-assessments.json` when driven through the new flow, not by calling `assess` directly per hit | **done** | Depends on Chain A (P9.5-A) — nobody will interactively review a sample if every step is slow. **This is the methodological heart of the whole gap**: the user's test session (483 raw hits, zero calibration) never touched `assess`/`calibrate`/`companions`/`compare`/`ablate` at all — it did one raw anchor census, then wrote ad hoc Python with a hand-rolled keyword-bucket "feature tagger" for clustering, none of which is Ontograph's actual methodology. Read this ledger row together with this session's own design instruction above before building — the shape of the flow is the deliverable, not just its mechanics. | **Implementation (design provisionally approved by the researcher — see .hermes/plans/2026-08-31_p95-guided-flow-design.md in the parent repo; amend, never overwrite):** new `src/ontograph/walk.py` + CLI verb `walk <study> --object <addr> [--sample N] [--seed S] [--script file.json] [--assessor id]`. Per-hit decisions (a/r/u) batch in-session and are written through ONE occurrence-ledger write (the same jsonl `assess` appends); `n:<form>` appends the refined anchor to the object's registered anchors and records an append-only `walk-anchor_narrowed` EventRecord (first-class, never a silent edit); `t` writes a real TraceRecord (P4.1 records) + `walk-trace_promoted` event; `w` widens the sample deterministically (seeded, doubling); `x`/`done`/Enter leave hits undecided — undecided hits are listed in the result and NEVER imputed into assessed-mode numbers (asserted: 1 accepted + 6 undecided → numerator 1, ambiguous_only 0). Non-TTY stdin without --script raises CLIError (exit 1, empty stdout — P5.3's no-silent-success contract). **Verify run:** TDD — 5 new tests in `tests/test_walk.py` written first and watched failing (SystemExit/CLIError, verb absent), then passing; the scripted fixture walkthrough of mirror's 7 hits reproduces `canonical-study-assessments.json` exactly (9101–9104+9201 accepted, 9105 ambiguous, 9106 rejected) THROUGH the flow, and the follow-up assessed-mode census returns 5/27 with accepted_poems [9101,9102,9103,9104,9201]. End-to-end smoke through the real `main()` CLI entry (not just the test harness): WALK summary 5/1/1, census 5/27, OK. Full suite: **153 passed, 10 skipped, 1 xfailed** (148 + 5 new). Split (`s`) in v0.1 records an event and requires the second object's registration via `object add` outside the walk — recorded as a scope note, not silently automated.
| P9.6 | Update `persian-poetry-ontograph/SKILL.md` to concretely script the P9.5 flow (not just name the `assess` verb in an example block, the way it currently does) | Part VII §41-44 | manually run the scripted flow against the fixture from a fresh session and confirm it matches P9.5's own test | **done** | Depends on P9.5. The user's test session had this skill unavailable anyway (see below) — once P9.1/PR-merge fix availability, the skill's own content must actually carry the flow, not just reference the verb. | **Implementation:** the skill's Invocation pattern now scripts `walk` as the DEFAULT assessment path — full choice grammar (a/r/u, n:<form> with anchor-revision semantics, t, w/x, Enter/done), the scripted `--script responses.json` replay form, and the P9.5 verify target stated inline (canonical 5/1/1 split, census 5/27); per-hit `assess` repositioned as the one-off-correction tool (latest-entry-wins), and `--mode assessed` paragraph updated to point at the walk flow as the way assessments are produced. **Verify run:** the scripted flow was re-run from a fresh session against the fixture using exactly the skill's documented command sequence (study new → object add → walk --script → census --mode assessed): canonical split reproduced (5/1/1), assessed census 5/27 with accepted_poems [9101,9102,9103,9104,9201] — matches P9.5's own test. Full suite unchanged: 153 passed / 10 skipped / 1 xfailed.
| P9.7 | Fixture-level test for a full calibrate→guided-assess→assessed-mode-query cycle at a realistic scale (reuse the HEART object's 41 hits/11 poems, not just mirror's 7) | §9, §27.2, §70 | assessed-mode `census`/`companions` results after the P9.5 flow match hand-verified ground truth on the HEART object, the same rigor as P7.5's scalability gate but exercised through the new guided flow instead of direct `assess` calls | todo | Depends on P9.5, P9.6. |
| P9.8 | `ontograph release` (or a new lightweight verb) renders what's actually in the workspace — Profiles, Findings, comparison/ablation numbers, Traces — as artifact/HTML plus a Markdown report by default (per this session's own instruction; a different format only when the researcher asks) | §55, §56, Part VII | fixture-level test: a full Field Charter → calibrate → assess → compare → release cycle produces both a `.html` and a `.md` output whose numbers match the known fixture ground truth (e.g. mirror's 5/27 assessed prevalence, the mirror×rust co-incidence divergence) | todo | Depends on P9.5–P9.7 (needs real assessed data and Findings to render, not placeholder content). User's test session and your own manual test both independently hit this same gap — no Research Release rendering path existed, so both times a generic charting skill got improvised from scratch instead. |
| P9.9 | Chain-B closeout: full existing suite + P9.5–P9.8 tests all green; one full real-corpus dry run structurally similar to the جن task (a mid-size hit set, not just the fixture) through the entire guided flow to a rendered release, checked by a human (not self-certified) | §69 (extended) | full suite green; a human (you) reviews the real-corpus dry run's rendered output before calling this phase done | todo | Depends on P9.5–P9.8. This is the actual "ready for another round of testing" checkpoint — per the same discipline as Phase 8, an agent should not declare this phase done on its own say-so; it hands you real output and stops. |

## Out of loop scope (do not add rows for these without a Research Situation justifying them — §73–74)

Mediation/relation-mediated-thickness (v0.2, §72), multiplex graph
diagnostics beyond bridge-diagnostic ablation, matrix decomposition,
spectral/community diagnostics, any browser workbench, any graph database,
corpus-wide LLM labeling, a universal Persian lemmatizer.
