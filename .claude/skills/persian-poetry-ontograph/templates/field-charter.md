# Field Charter Template

Create or revise only after a ResearchSituation exists. If the Field changes after inquiry evidence was verified, run `ontograph inquire --refresh <catalog-id>` before review/promotion.

- research_purpose:
- corpus_snapshot: exact commit and manifest hash; never "latest"
- inclusion_rule:
- exclusion_rule:
- active_encounter_scales:
- empirical_temporal_scope: include proxy rule if derived
- empirical_spatial_or_institutional_scope:
- known_blind_zones:
- reopening_conditions:

Derived scope is labelled derived in researcher-facing text and carries the exact rule. Do not present a poet-life proxy as poem composition date.