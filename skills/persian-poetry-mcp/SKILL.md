---
name: persian-poetry-mcp
description: Moved. See .claude/skills/persian-poetry/SKILL.md for the current, corrected version of this skill.
version: 1.0.0
author: Erfan Bashar
license: MIT
---

# Moved

This skill relocated to
[`.claude/skills/persian-poetry/SKILL.md`](../../.claude/skills/persian-poetry/SKILL.md)
so Claude Code auto-discovers it (it only scans `.claude/skills/`). That
version also corrects a few details (e.g. the `get` tool's parameter is
`file`, not `docid`; `query` needs `"rerank": false` in offline/CPU-only
environments). Use that file going forward — this one is kept only so old
references don't break.
