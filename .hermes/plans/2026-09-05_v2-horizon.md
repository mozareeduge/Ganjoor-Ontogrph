# Horizon 2026-09-05 — Ganjoor-Ontograph to v0.2.0

State: branch `ontograph/v0.1-build` @ d28022caf, suite **253/10/1 green**.
Done: T01–T13 (v0.1.1, Gates A–D), U03, W01–W06, P0–P8, P9 Chain A+B except P9.9.
Truth sources: HANDOFF_2026-09-04.md + IMPLEMENTATION_LEDGER.md (§13 tables in
V0_2 spec still say `todo` everywhere — stale, ignore; ledger/handoff rule).

## Hole status (re-evaluated 2026-09-05)

- External-review Finding 1 (co-incidence untested): CLOSED — P3.3/P3.8 assert
  anchor (4/3) vs assessed (3/2) divergence on poem 9106; CLI re-guarded (P8.3).
- Finding 2 (estimator unfalsifiable): CLOSED at fixture — P2.4/P7.5 HEART
  object (41 hits), Wilson+FPC, multi-seed bracket asserts.
- Finding 3 (substring truth, unverified schema): CLOSED at fixture — P1.2
  token-exact canary (9107), P0.5 real-schema spot check (epic SectionIndex1
  warning recorded for Phase 8-scale work).
- Residual risks → rows: real-corpus proof (P9.9/U10/U11/V212), human
  calibration link (W08/U11 Gate G), bypass recurrence (W07A-B/U04-U05),
  relation guards unbuilt (V202/V207/V206/V208), catalog rigor (W10/W11),
  distribution (U06-U10/U12/V213).

## Binding order (§19.8)

P9.9 → U01 → U02 → W07A → W07B → W08 → U04 → U05 → W09A → W09B →
U06 → U07 → U08 → U09 → U10 → U11 (Gate G, human) → U12 (v0.1.2) →
V201 → V202 → V207 → V203 → V204 → V205 →
W10A → W10B → W11A → W11B →
V206 → V208 → V209 → V210 → V211 → V212 (Gate H, human) → V213 (v0.2.0)

One row per turn: discriminating test first (observe fail) → implement →
full suite green → `git diff --check` → one commit `Ontograph <row>: …` → push.
Auto-continue; pause only for Gate G/H, real blockers, destructive actions.

## Skill wiring (all present locally, verified 2026-09-05)

- test-driven-development: every row (existing protocol, unchanged).
- systematic-debugging: real-corpus rows (P9.9, U10, U11) — P8-style edge cases.
- delegated-plan-review (Codex): W10A, V202, V207 designs before build.
- openspec-propose/apply-change: W07–W11, V-series spec deltas.
- requesting-code-review: pre-commit on U12, V213.
- mozare-dev-collage: U08 packaging, U09 CI (Windows+Ubuntu, py3.10/3.12).
- handover-qa-discipline: U11 Gate G, V212 Gate H handoffs.
- webapp-release-gates: U12, V210/V213 release gates.
- windows-python-pitfalls + windows-shell-pitfalls: file-locking, path, venv.
- persian-poetry-ontograph (repo skill): rewrite target of U06/V211.

## Notes

- Never touch: `poets/`, `index/`, `manifest.json`, `.hermes/`,
  `ontograph-workspaces/` (untracked, preserve).
- Known hygiene debt: `fixtures/mini-ganjoor/ontograph-support-idx.sqlite`
  untracked in-tree; support-index should move under workspace/cache before
  real-corpus runs. Manifest drift: scan finds 132,538 poems vs manifest
  132,591 — reconciled (stale top-level count), engine count is canonical.
- Test cmd: `cd ganjoor-ontograph && ./.venv/Scripts/python.exe -m pytest -q`
  (bare; `pytest tests/` misses fixture ground truth).
